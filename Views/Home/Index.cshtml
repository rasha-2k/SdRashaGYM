@* @{
    Layout = "~/Views/Shared/_Layout.cshtml";
} *@
<!-- Background Image Div -->
<div class="background-image"></div>
<!-- CSS -->
<style>
    /* Reset body styles */
    body {
        margin: 0;
        padding: 0;
        font-family: Arial, sans-serif;
        background-color: #fff; /* Backup color in case image fails */
    }

    /* Background Image Div */
    .background-image {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100vh;
        background-image: url('https://images.pexels.com/photos/669584/pexels-photo-669584.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=1');
        background-size: cover;
        background-position: center center;
        background-repeat: no-repeat;
        z-index: -1;
    }

    /* Main Container */
    .main-container {
        top:99px;
        position: relative;
        z-index: 1;
        padding: 20px;
        max-width: 1200px;
        margin: 20px;
        text-align: center;
        border-radius: 10px;
    }

    /* Header Styling */
    .header {
        margin-bottom: 40px;
    }

        .header h1 {
            font-size: 36px;
            color: #ff4040;
            margin-bottom: 10px;
        }

        .header p {
            font-size: 18px;
            color: black;
        }

    /* Dashboard Styling */
    .dashboard {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 20px;
    }

    .card {
        background-color: #2b2f38;
        border-radius: 10px;
        padding: 20px;
        text-align: center;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        transition: transform 0.3s, background-color 0.3s;
    }

        .card:hover {
            transform: translateY(-10px);
            background-color: #343a46;
        }

    .card-icon {
        font-size: 50px;
        color: #ff4040;
        margin-bottom: 15px;
    }

    .card h3 {
        font-size: 24px;
        color: #f2f2f2;
        margin-bottom: 10px;
    }

    .card p {
        font-size: 16px;
        color: #b3b3b3;
        margin-bottom: 20px;
    }

    .card-button {
        display: inline-block;
        background-color: #ff4040;
        color: #fff;
        padding: 10px 20px;
        border-radius: 5px;
        text-decoration: none;
        font-size: 16px;
        transition: background-color 0.3s;
    }

        .card-button:hover {
            background-color: #e03535;
        }
</style>

<!-- Main Content -->
<div class="main-container">
    <div class="header">
        <h1>Welcome to SdRasha Gym!</h1>
        <p>Efficiently manage your gym's users and packages.</p>
    </div>

    <div class="dashboard">
        <div class="card">
            <i class="fas fa-users card-icon"></i>
            <h3>Manage Users</h3>
            <p>View, add, and manage gym users.</p>
            <a asp-controller="Account" asp-action="Register" class="card-button">Add User</a>
            <a asp-controller="Users" asp-action="Index" class="card-button">View Data</a>
        </div>

        <div class="card">
            <i class="fas fa-dumbbell card-icon"></i>
            <h3>Manage Packages</h3>
            <p>Customize membership packages for users.</p>
            <a asp-controller="Home" asp-action="AddPackage" class="card-button">Add Package</a>
            <a asp-controller="Home" asp-action="PackageList" class="card-button">View Data</a>
        </div>
    </div>
</div>

